# slow and fast pointer

-- to cycle problem
-- to find node in cycle etc

fast = node.next.next; // +2
slow = node.next // +1

# BubbleSorrt :- After Every Iteration the Largest Element Comes In The End
