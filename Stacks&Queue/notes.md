# Stack :-- LIFO, FILO

stack extends a vector class
internally Stacks working as a Array

# Queue :-- LILO, FIFO

internally queeu uses LinkedList

to convert recursive programs to iteratives we use stacks and queue

# Deque :- Doubly inded Queue

insert and remove from both the side
ReSizeble Array Is the internal Implementations of the deque
